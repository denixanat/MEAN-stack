import { Component, EventEmitter, Output } from '@angular/core';

@Component({
  selector: 'app-barra',
  templateUrl: './barra.component.html',
  styleUrls: ['./barra.component.css']
})
export class BarraComponent {
  /* @Output()
  public changeModule : EventEmitter<number> = new EventEmitter();

  public showModule(valor : number) : void{
    this.changeModule.emit(valor);
  } */
}
